import { codifyClick } from '~/utils'

const SolutionsPointsContainer = (props) => {
  const { persona, children } = props

  return <div className='solutions-points-container'>{children}</div>
}

export default SolutionsPointsContainer
